<%= form_for(@parapluie) do |f| %>
  <% if @parapluie.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@parapluie.errors.count, "error") %> prohibited this parapluie from being saved:</h2>

      <ul>
      <% @parapluie.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :colour %><br />
    <%= f.text_field :colour %>
  </div>
  <div class="field">
    <%= f.label :foldable %><br />
    <%= f.check_box :foldable %>
  </div>
  <div class="field">
    <%= f.label :handle_type %><br />
    <%= f.text_field :handle_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
